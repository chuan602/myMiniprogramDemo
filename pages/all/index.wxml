<!--index.wxml-->
<view class="container">
  <list-item 
    wx:for="{{ dataList }}"
    wx:key="id"
    content="{{ item.content }}"
    department="{{ item.department }}"
    extra="查看"
  />
</view>
